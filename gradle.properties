android.useAndroidX=true
android.enableJetifier=true

# Optional: set so the app fetches your Steam library automatically after sign-in (no need to add key in Settings).
# Get a key at https://steamcommunity.com/dev/apikey (use e.g. localhost as domain).
# STEAM_WEB_API_KEY=your_key_here
